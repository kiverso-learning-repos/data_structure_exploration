class BinarySearchTree
  attr_reader :head
  def initialize
    @head = nil
  end

  def insert(score, title)
    if @head = nil
      @head = Node.new(score, title)
    else
      if @score < @head.
    end

  end
end